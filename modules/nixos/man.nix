{
  documentation = {
    man.generateCaches = true; # enable stuff like man -k and apropos
    dev.enable = true; # enable documentation targeted towards developers
    doc.enable = true; # to install package documentation in /share/doc. In plain text/HTML
  };
}
